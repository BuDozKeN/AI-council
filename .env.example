# Supabase Configuration
VITE_SUPABASE_URL=https://your-project.supabase.co
VITE_SUPABASE_ANON_KEY=your-anon-key-here

# Backend API
VITE_API_URL=http://localhost:8000

# CORS Configuration (required for production)
# Comma-separated list of allowed origins
# Development: Leave empty to use localhost defaults
# Production: Set to your frontend domain(s)
# CORS_ORIGINS=https://app.example.com,https://staging.example.com
CORS_ORIGINS=

# Sentry Error Tracking (optional)
# Frontend (Vite)
VITE_SENTRY_DSN=
# Backend (FastAPI) - set in Render environment variables
SENTRY_DSN=

# Environment (production/development)
ENVIRONMENT=development

# Feature Flags (optional)
VITE_ENABLE_ANALYTICS=false
VITE_ENABLE_DEBUG_MODE=false

# Redis Caching (optional)
# Used for: LLM response caching, rate limiting, session caching
# Development: docker run -d -p 6379:6379 redis
# Production: Use managed Redis (Render, Railway, Upstash)
# Set REDIS_ENABLED=false to disable caching entirely
REDIS_URL=redis://localhost:6379/0
REDIS_ENABLED=true
REDIS_DEFAULT_TTL=3600
REDIS_LLM_CACHE_TTL=1800

# Qdrant Vector Database (optional)
# Used for: Semantic search, RAG retrieval, knowledge embeddings
# Development: docker run -d -p 6333:6333 qdrant/qdrant
# Production: Use Qdrant Cloud (https://cloud.qdrant.io)
# Set QDRANT_ENABLED=false to disable vector search entirely
QDRANT_URL=http://localhost:6333
QDRANT_API_KEY=
QDRANT_ENABLED=true

# ════════════════════════════════════════════════════════════════════════════════
# SECURITY: API Key Encryption
# ════════════════════════════════════════════════════════════════════════════════
# BYOK (Bring Your Own Key) encryption uses HKDF for per-user key derivation.
# CRITICAL: Set these in production for proper key security.

# Master encryption key for user API keys (Fernet format)
# Generate with: python -c "from cryptography.fernet import Fernet; print(Fernet.generate_key().decode())"
# USER_KEY_ENCRYPTION_SECRET=your-fernet-key-here

# HKDF salt for key derivation (hex-encoded, 32 bytes recommended)
# Generate with: python -c "import secrets; print(secrets.token_hex(32))"
# PRODUCTION REQUIRED: Set this to a unique value for your deployment
# HKDF_SALT=your-64-char-hex-salt-here

# Subscription Pricing (values in cents)
# These configure the subscription tier pricing and query limits
# Defaults are sensible for most deployments - uncomment to override
# SUBSCRIPTION_FREE_QUERIES=5
# SUBSCRIPTION_PRO_PRICE=2900
# SUBSCRIPTION_PRO_QUERIES=100
# SUBSCRIPTION_ENTERPRISE_PRICE=9900
# SUBSCRIPTION_ENTERPRISE_QUERIES=-1

# ════════════════════════════════════════════════════════════════════════════════
# STAGING ENVIRONMENT (for scripts/seed_staging.py and scripts/clone_to_staging.py)
# ════════════════════════════════════════════════════════════════════════════════
# Create a SEPARATE Supabase project for staging (free tier works)
# This isolates test data from production users

# Staging Supabase (MUST be different from production!)
# STAGING_SUPABASE_URL=https://your-staging-project.supabase.co
# STAGING_SUPABASE_SERVICE_KEY=eyJ...staging-service-key

# Production keys (for clone_to_staging.py - read from production)
# Only needed when running the clone script
# SUPABASE_URL=https://your-production.supabase.co
# SUPABASE_SERVICE_KEY=eyJ...production-service-key
